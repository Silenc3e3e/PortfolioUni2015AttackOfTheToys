using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class ChangeTextToInputName : MonoBehaviour {

	public Text TextBox;

	void Start () {
		if(TextBox!=null)
			TextBox.text=Input.GetAxis("Evade").ToString();
	}
}
